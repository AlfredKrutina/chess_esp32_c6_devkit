## IDF Component Manager Manifest File
dependencies:
  espressif/led_strip: ^3.0.1~1
  # All dependencies of `main` are public by default.
  # LED strip is optional - only required for hardware mode
  # Simulation mode uses no external dependencies
  # espressif/led_strip: ^3.0.1~1  # Uncomment for hardware mode
  espressif/esp_matter: ^1.4.0~1
