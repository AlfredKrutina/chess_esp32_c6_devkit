Projekt přidává SPM závislost https://github.com/eastriverlee/LLM.swift (llama.cpp + Metal). Balíček používá Swift makra — při prvním buildu v Xcode potvrď důvěru v makra (Product → Build), případně: defaults write com.apple.dt.Xcode IDESkipMacroFingerprintValidation -bool YES

Model ulož jako coach_gemma.gguf do Application Support/ChessCoachModels/ (viz ModelDownloadManager.resolvedModelFileURL).
