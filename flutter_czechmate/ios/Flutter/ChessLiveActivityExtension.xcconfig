// Hodnoty musí sedět s hlavní aplikací (pubspec version).
// Fallback pod `flutter clean`, dokud znovu nevznikne Generated.xcconfig (`flutter pub get` / build).
FLUTTER_BUILD_NAME = 1.1.0
FLUTTER_BUILD_NUMBER = 1
#include? "Generated.xcconfig"
